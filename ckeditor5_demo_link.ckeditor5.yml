ckeditor5_demo_link_demoLink:

  ckeditor5:
    plugins:
      - demoLink.DemoLink

  drupal:
    label: Demo Link

    # Drupal library with the plugin JS.
    library: ckeditor5_demo_link/demoLink

    # Toolbar button.
    toolbar_items:
      DemoLink:
        label: DemoLink

    # HTML elements tp attach the plugin to.
    elements:
      - <p>
